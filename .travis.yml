language: node_js
node_js:
  - "6.9"

branches:
  only:
    - "master"
    - "develop"

before_script:
  - npm install -g firebase-tools
  - npm install -g @angular/cli

script:
  - ng build --prod --aot
  
deploy:
  provider: firebase
  project: mychatapp-mf
  token:
    secure: "FIREBASE_TOKEN"
  on:
    branch: "master"
  skip_cleanup: true
    
cache:
  directories:
  - node_modules
